#include <stdio.h>

/**
 * main - where it goes down
 * Printf: prints some strings
 * Return: return zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
